/*
* Also. Im still working in progress with the Theme Saver that works easily at the ass.
*
* l
*
*
*
*/

const nodefs = require('node:fs');
const nodezip = require('node-zip');
const { kill } = require('node:process');
const { get } = require('node:http');






exports.themesaver.Save = function() {
    if (themeSaved) {
        arguments = function(res, req) {
            nodefs.readFile('../../../tools/neko/nekoc.exe', (res, err)) = {

            }
        }
    }
}