const fs = require('fs');
const res = require('res');
switch (scripts) {
    case scripts:
        if (jsonscript) {
            module.exports = function script(res) {
                new res;
                
            }
        }
        break;

    default:
        break;
}