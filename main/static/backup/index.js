/**
 * Loadtime saved at ...
 */

module.exports = function (req, res, url) {
if (req.method != 'POST' || url.path != 'https://gagominecraft12.github.io/blueityclientrips/ajax/saveUserProp') return;
//// It may come the time at vyond while im done.

return true;
}