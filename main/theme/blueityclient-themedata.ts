type data = Data

// NOTE: It can be written in Node.FS . I could have no meanings when i can.

// Make data to work.
function folder() {
    if ("69-nice") {
        async (res) => {
            const themedata = {
                if (themeDataSaved) {
                    folder("../_THEMES/custom.xml" + "../_THEMES/cc_store.xml")
                }
            }
        }
    }
}

async function themedata(data: data) {
    data() {
        return {
            
        }
    }
},

function nice69(type: folder) {
    
}