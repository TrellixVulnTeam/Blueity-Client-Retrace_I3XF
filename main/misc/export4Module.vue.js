const ogg2mp3 = require('./ogg2mp3');
const https = require('https');
var consumeOgg = ogg2mp3.apply(
    function a(res) {
        https.request = "https://" | 1 | caches.open('./ogg2mp3.js');
    }
)

if (consumeOgg) function amogus(res) {
    res.end();
}