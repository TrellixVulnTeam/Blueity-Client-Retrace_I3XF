const fw = process.env.FILE_WIDTH - 0;
const fs = require('fs');
const execShell = require('child_process').execSync;

`script = no=flash yes=js reference=flash , js , lua , go`

var repo, remote, index;
var tornillos = {
    swfReference = 'go_full.swf',
    swfCCStoreReference = '/store/cc_store/$name$/',
    // Set a Language `eng`, `spa`, `fre`, `kor`, `mex`
    set = `eng`
}

module.exports = {
    getValues(
        fetch = `http://ga.vyond.com/goapi/SaveSound`,
        fetch = `http://ga.vyond.com/goapi/getUserWatermarks`,
        tornillos = true
    );
}